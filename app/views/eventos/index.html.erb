<div class="container">
  <h2>Eventos</h2>
  <div class="eventos-index">
    <%= render 'eventos', eventos: @eventos %>
  </div>
  <% if current_user && current_user.pode_postar_evento? %>
    <%= link_to glyphicon('plus') + " Novo evento", new_evento_path, class: "btn btn-success" %>
  <% end %>
</div>
